globalThis.process ??= {};
globalThis.process.env ??= {};
import { c as createComponent } from "./astro-component_Dn7U0_eq.mjs";
import { r as renderTemplate, m as maybeRenderHead, F as Fragment, a as addAttribute } from "./sequence_I_kcixDX.mjs";
import { r as renderComponent } from "./worker-entry_Cv5GlnJ5.mjs";
import { env } from "cloudflare:workers";
import { $ as $$App } from "./App_ChWwyHiq.mjs";
import "./stripe_r-RFTlbb.mjs";
import { atLeast } from "./index_CrjiuAkj.mjs";
var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(cooked.slice()) }));
var _a;
const prerender = false;
const $$Theme = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$props, $$slots);
  Astro2.self = $$Theme;
  const { getSession } = await import("./auth_BXoLTJDQ.mjs");
  const ses = await getSession(env, Astro2.request);
  if (!ses || ses.role !== "admin" || ses.ctx !== "org") return Astro2.redirect("/login", 302);
  const entitlement = await (await import("./client_DsX87Mps.mjs")).cachedEntitlement(env);
  const hasPlus = atLeast(entitlement, "plus");
  const { getTheme } = await import("./theme_DO0iS6ur.mjs");
  const theme = hasPlus ? await getTheme(Astro2.locals.ctx) : {};
  return renderTemplate`${renderComponent($$result, "App", $$App, { "title": "ブランド設定", "active": "/apps" }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<h1>ブランド設定（見た目）</h1> ${!hasPlus && renderTemplate`<div class="card"> <div class="banner banner-warn">この機能は <strong>Plus 以上</strong>のプランで利用できます。</div> <p class="muted">ブランド名・ロゴ・配色を団体ごとに上書きできます。</p> <a class="btn btn-primary" href="/billing">プラン・課金へ</a> </div>`}${hasPlus && renderTemplate`${renderComponent($$result2, "Fragment", Fragment, {}, { "default": async ($$result3) => renderTemplate` <p class="muted">ブランド名・ロゴ・配色を団体ごとに上書きします（共通の画面構造はそのまま）。空欄は既定値。色は #hex 等で指定。</p> <div class="card"> <div class="field"><label>ブランド名<input id="th-brand"${addAttribute(theme.brand ?? "", "value")} placeholder="baku-office"></label></div> <div class="field"> <label>ロゴ画像<input id="th-logo"${addAttribute(theme.logoUrl ?? "", "value")} placeholder="https://…/logo.png または下からアップロード"></label> <div class="muted" style="font-size:.8rem;margin-top:4px">空欄でブランド名のテキスト表示。すでにWeb上にあるロゴなら画像URL（https://… で終わりが .png/.jpg）を貼り付け、無ければ下の「アップロード」でお手元の画像から設定できます。</div> <div class="row" style="align-items:center;gap:12px;margin-top:8px"> <img id="th-logo-prev"${addAttribute(theme.logoUrl || "", "src")} alt="ロゴプレビュー"${addAttribute(`height:32px;max-width:160px;object-fit:contain;background:var(--surface-2);border:1px solid var(--line);border-radius:var(--r-sm);padding:2px 6px;${theme.logoUrl ? "" : "display:none"}`, "style")}> </div> <div class="row" style="gap:.5rem;margin-top:8px"><input id="th-logo-file" type="file" accept="image/png,image/jpeg,image/webp,image/gif" style="flex:1" aria-label="ロゴの画像ファイル"><button class="btn btn-sm" type="button" id="th-logo-up" style="flex:0 0 auto">アップロード</button><button class="btn btn-sm" type="button" id="th-logo-clear" style="flex:0 0 auto">ロゴを消す</button></div> <div class="muted" style="font-size:.82rem;margin-top:4px">PNG/JPEG/WebP/GIF・3MBまで。アップロードすると即座にロゴURLへ反映されます。</div> </div> <div class="field"> <label>相棒（マスコット）画像URL<input id="th-mascot"${addAttribute(theme.mascotUrl ?? "", "value")} placeholder="空欄=既定（貘キャラ）／ https://…/your.png"></label> <div class="muted" style="font-size:.8rem;margin-top:4px">空欄で合同会社 貘の既定キャラ。背景透過のPNG（正方形〜横長）推奨。自社キャラの画像URLを指定すると相棒が差し替わります。</div> <div class="row" style="align-items:center;gap:12px;margin-top:8px"> <img id="th-mascot-prev"${addAttribute(theme.mascotUrl || "/mascot/baku.png", "src")} alt="相棒プレビュー" style="width:76px;height:76px;object-fit:contain;background:var(--surface-2);border:1px solid var(--line);border-radius:var(--r-sm)"> <button class="btn btn-sm" type="button" id="th-mascot-default" style="flex:0 0 auto">既定に戻す</button> </div> </div> <div class="field"> <label>AIで相棒を生成（Workers AI）</label> <div class="row" style="gap:.5rem"><input id="th-mascot-prompt" placeholder="例：青い貘の赤ちゃん、和柄、かわいい、シンプル" style="flex:1"><button class="btn btn-primary btn-sm" type="button" id="th-mascot-gen" style="flex:0 0 auto">生成して設定</button></div> <div class="muted" style="font-size:.78rem;margin-top:4px">説明を入れると相棒画像を生成し、そのまま設定します（数秒〜十数秒）。CF上の Workers AI で生成します。</div> </div> <div class="field"> <label for="th-mascot-file">画像をアップロードして設定</label> <div class="row" style="gap:.5rem"><input id="th-mascot-file" type="file" accept="image/png,image/jpeg,image/webp,image/gif" style="flex:1" aria-label="相棒の画像ファイル"><button class="btn btn-sm" type="button" id="th-mascot-up" style="flex:0 0 auto">アップロード</button></div> <div class="muted" style="font-size:.82rem;margin-top:4px">PNG/JPEG/WebP/GIF・3MBまで。背景透過のPNGが綺麗に表示されます。</div> </div> <div class="muted" style="font-size:.8rem;margin:.4rem 0">色は左のパレットで選ぶか、右に #C9A86A のような色コードを直接入力できます（空欄で既定色）。</div> <div class="row"> <label style="flex:1">ブランド色<span class="th-color-row"><input type="color" class="th-color-pick" data-for="th-brand-color"${addAttribute(theme.colors?.brand || "#C9A86A", "value")} aria-label="ブランド色を選ぶ"><input id="th-brand-color"${addAttribute(theme.colors?.brand ?? "", "value")} placeholder="#C9A86A"></span></label> <label style="flex:1">背景色<span class="th-color-row"><input type="color" class="th-color-pick" data-for="th-bg"${addAttribute(theme.colors?.bg || "#EFE9DC", "value")} aria-label="背景色を選ぶ"><input id="th-bg"${addAttribute(theme.colors?.bg ?? "", "value")} placeholder="#EFE9DC"></span></label> <label style="flex:1">文字色<span class="th-color-row"><input type="color" class="th-color-pick" data-for="th-ink"${addAttribute(theme.colors?.ink || "#0E1A2B", "value")} aria-label="文字色を選ぶ"><input id="th-ink"${addAttribute(theme.colors?.ink ?? "", "value")} placeholder="#0E1A2B"></span></label> </div> <button class="btn btn-primary" id="saveTheme">テーマを保存</button> </div>  <style>
        .th-color-row { display: flex; align-items: center; gap: 6px; }
        .th-color-row input[type="color"] { width: 44px; height: 36px; padding: 0; border: 1px solid var(--line); border-radius: var(--r-sm); background: none; flex: 0 0 auto; cursor: pointer; }
        .th-color-row input:not([type="color"]) { flex: 1; min-width: 0; }
      </style> `, "scripts": async ($$result3) => renderTemplate(_a || (_a = __template(['<script slot="scripts">\n        (function () {\n          const btn = document.getElementById("saveTheme");\n          if (!btn) return;\n          const mascot = document.getElementById("th-mascot");\n          const prev = document.getElementById("th-mascot-prev");\n          mascot?.addEventListener("input", () => { if (prev) prev.src = mascot.value.trim() || "/mascot/baku.png"; });\n\n          // 色：パレット(type=color)とテキスト(#hex)を双方向同期。保存値はテキスト欄を正とする。\n          document.querySelectorAll(".th-color-pick").forEach((pick) => {\n            const txt = document.getElementById(pick.dataset.for);\n            if (!txt) return;\n            if (txt.value.trim()) pick.value = txt.value.trim();\n            pick.addEventListener("input", () => { txt.value = pick.value; });\n            txt.addEventListener("input", () => { if (/^#[0-9a-fA-F]{6}$/.test(txt.value.trim())) pick.value = txt.value.trim(); });\n          });\n\n          // ロゴ：プレビュー追従。\n          const logo = document.getElementById("th-logo");\n          const logoPrev = document.getElementById("th-logo-prev");\n          const showLogo = (u) => { if (!logoPrev) return; if (u) { logoPrev.src = u; logoPrev.style.display = ""; } else { logoPrev.style.display = "none"; } };\n          logo?.addEventListener("input", () => showLogo(logo.value.trim()));\n          // ロゴ：アップロード（mascot と同方式）。即座に logoUrl へ反映。\n          document.getElementById("th-logo-up")?.addEventListener("click", async (e) => {\n            const f = document.getElementById("th-logo-file").files?.[0];\n            if (!f) { window.bo.toast("ファイルを選択してください", "err"); return; }\n            if (f.size > 3 * 1024 * 1024) { window.bo.toast("画像は3MBまでです", "err"); return; }\n            const dataB64 = await new Promise((res, rej) => { const rd = new FileReader(); rd.onload = () => res(String(rd.result).split(",")[1] || ""); rd.onerror = rej; rd.readAsDataURL(f); });\n            const r = await window.bo.api("/api/logo", { _action: "upload", dataB64, mime: f.type }, { btn: e.currentTarget, successMsg: null });\n            if (r.ok) { window.bo.toast("ロゴを設定しました"); if (logo) logo.value = r.data.logoUrl; showLogo(r.data.logoUrl); setTimeout(() => location.reload(), 1000); }\n          });\n          document.getElementById("th-logo-clear")?.addEventListener("click", async (e) => {\n            const r = await window.bo.api("/api/logo", { _action: "reset" }, { btn: e.currentTarget, successMsg: "ロゴを消しました" });\n            if (r.ok) { if (logo) logo.value = ""; showLogo(""); setTimeout(() => location.reload(), 700); }\n          });\n          document.getElementById("th-mascot-default")?.addEventListener("click", async (e) => {\n            const r = await window.bo.api("/api/mascot", { _action: "reset" }, { btn: e.currentTarget, successMsg: "既定に戻しました" });\n            if (r.ok) { if (mascot) mascot.value = ""; if (prev) prev.src = "/mascot/baku.png"; setTimeout(() => location.reload(), 700); }\n          });\n          // AI生成（Workers AI）\n          document.getElementById("th-mascot-gen")?.addEventListener("click", async (e) => {\n            const prompt = (document.getElementById("th-mascot-prompt").value || "").trim();\n            if (!prompt) { window.bo.toast("どんなキャラにするか入力してください", "err"); return; }\n            const r = await window.bo.api("/api/mascot", { _action: "generate", prompt }, { btn: e.currentTarget, successMsg: null });\n            if (r.ok) { window.bo.toast("相棒を生成しました"); if (mascot) mascot.value = r.data.mascotUrl; if (prev) prev.src = r.data.mascotUrl; setTimeout(() => location.reload(), 1000); }\n          });\n          // アップロード\n          document.getElementById("th-mascot-up")?.addEventListener("click", async (e) => {\n            const f = document.getElementById("th-mascot-file").files?.[0];\n            if (!f) { window.bo.toast("ファイルを選択してください", "err"); return; }\n            if (f.size > 3 * 1024 * 1024) { window.bo.toast("画像は3MBまでです", "err"); return; }\n            const dataB64 = await new Promise((res, rej) => { const rd = new FileReader(); rd.onload = () => res(String(rd.result).split(",")[1] || ""); rd.onerror = rej; rd.readAsDataURL(f); });\n            const r = await window.bo.api("/api/mascot", { _action: "upload", dataB64, mime: f.type }, { btn: e.currentTarget, successMsg: null });\n            if (r.ok) { window.bo.toast("相棒を設定しました"); if (mascot) mascot.value = r.data.mascotUrl; if (prev) prev.src = r.data.mascotUrl; setTimeout(() => location.reload(), 1000); }\n          });\n          btn.addEventListener("click", async (e) => {\n            const theme = { brand: document.getElementById("th-brand").value, logoUrl: document.getElementById("th-logo").value, mascotUrl: mascot ? mascot.value : "", colors: { brand: document.getElementById("th-brand-color").value, bg: document.getElementById("th-bg").value, ink: document.getElementById("th-ink").value } };\n            const r = await window.bo.api("/api/settings", { _action: "ui_theme", theme }, { btn: e.currentTarget, successMsg: "テーマを保存しました" });\n            if (r.ok) setTimeout(() => location.reload(), 700);\n          });\n        })();\n      <\/script>']))) })}`}` })}`;
}, "/home/runner/work/baku-office/baku-office/apps/client/src/pages/settings/theme.astro", void 0);
const $$file = "/home/runner/work/baku-office/baku-office/apps/client/src/pages/settings/theme.astro";
const $$url = "/settings/theme";
const _page = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: $$Theme,
  file: $$file,
  prerender,
  url: $$url
}, Symbol.toStringTag, { value: "Module" }));
const page = () => _page;
export {
  page
};
